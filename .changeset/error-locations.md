---
'@crypte/cli': patch
---

Syntax errors in story and component files give their line and column. `crypte check` names a component file that does not parse, and the preview names the story or reloading component that imports a name its module does not export.
