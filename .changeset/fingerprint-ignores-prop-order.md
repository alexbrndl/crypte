---
'@crypte/cli': patch
---

Reordering the props of a story no longer changes `.crypte/fingerprint.json`. Every committed fingerprint changes once with this version: run `crypte dev` and commit it.
