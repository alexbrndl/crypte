---
'@crypte/cli': patch
---

A component that breaks while the dev server runs now shows its error in the shell, instead of the version from before the edit marked as rendered. Once it reloads, the error goes.
